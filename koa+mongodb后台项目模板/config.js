/**
 * 配置文件
 */

// 路由配置
const net_Port = 3000;

// 数据库配置

const DB_Config = {
  url: 'localhost',
  port: '27017',
  dbName: 'mytest',
  userName: '',
  password: '',
};

module.exports = {
  DB_Config,
};
